<main>
    <article id="homepage">
        <section>
            <h1 id="welcome-heading"></h1>
            <button class="btn btn-primary" onclick="SPA.handlers.beginQuiz()">Begin Quiz!</button>
            <button class="btn btn-primary" onclick="SPA.navigatePage('results')">results shortcut!</button>
        </section>
    </article>
</main>
